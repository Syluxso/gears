# Backlog

Operational work items live here (or purely in the DB for lightweight items).

Use `gears backlog new`, `claim`, `complete`, `current`, `prioritize`.

See `.gears/backlog/` for any rich description files (optional).
